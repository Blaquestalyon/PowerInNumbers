[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["echo 'No dependencies — using Node built-ins only'"]

[phases.build]
cmds = ["echo 'Pre-built dist/ — no build step needed'"]

[start]
cmd = "node server.js"
