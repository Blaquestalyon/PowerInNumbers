{
  "name": "power-in-numbers",
  "version": "1.1.0",
  "description": "Power In Numbers — pre-built static deploy. Built artifact only — no Astro build step.",
  "private": true,
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "start": "serve dist -l ${PORT:-3000}",
    "build": "echo 'Pre-built — skipping build step'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
